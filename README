Install the script and requirements globally::

    pip install -e .

Install to $HOME::

    pip install --user -e .

Run (from project root containing data folders)::

    dt_os_menu

hsl-map-server needs FONTSTACK_PASSWORD env variable, that's
populated from fontstack secret, key fontstack_password.
You can create it with::

    oc create secret generic fontstack--from-literal=fontstack_password=foobar
